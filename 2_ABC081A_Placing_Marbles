#include <bits/stdc++.h>
using namespace std;

int main() {

    int cnt = 0;
    string a;
    cin >> a;

    for(int i=0; i<3; i++) {
        char b = a[i];
        if(b=='1') cnt++;
    }

    cout << cnt;

    return 0;
}